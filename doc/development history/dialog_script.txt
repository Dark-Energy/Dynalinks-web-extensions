Писалось на чистом js.


var my_select_form 
объект, включающий динамическую форму выбора, в которую можнод добавлять и удалять элементы, а также выбирать активные (чего не умеет даеж vue).


create: function (id, parent_id) {

создаёт элемент select, затем присваивает ему свойство id, затем ищет элемент parent_id, в который приписывает беднягу.

set_change_listener 
После создания элемента создаётся обрабобтчик события onchange, он прячется где-то там внутри. Достать его можно только с помощью self.onchange, который вызывается внутри этого обработчика. 

set_options (options_list, selected)
самая простая функция, просто собирает текст и присваивает его innerHTML.

add_option
работает таким же образом, вызывая set_options


set_selected
вызывает set_option, только заменяя второй параметр


try_load
ПЫтается загрузить список категорий из хранилища browser.storage.local.




В конце создаём порт и ждём сообщений с описанеим очередных url, title, которые надо добавить куда-нибудь.