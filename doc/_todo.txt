Не работает форма правки для правки уже добавленных записей
Полностью переделать механизм избранного. Убрать поля favorite and favorite_text. Сделать новую таблицу features, куда добавлять записи вида

_id: uuid
href: url
text: title
category: category hash

Никакой связи с ихними дублями в основной базе данных не останется. Слишком сложно поддерживать. Чай не в реляционной СУБД работаем. 
//сделано, но связи с дублями всё же оставлены. Думаю, придётся делать подобие реляционной БД. Ну, не тащить же sqljs в дополнение. 


Изменить кнопки сортировки на именно что кнопки, добавить сортировку по позиции вкладки. Требуется добавить в информацию о вкладках их позицию. Для этого надо переделать Tab_Manager.get_all_tabs_info и формы tab-list.
//сделано
 

Добавить экспорт содержимого storage через скрипт, впрыснутый с помощью приложения в постороннюю страницу. А вдруг там сработает SaveAs. 
//SaveAs выброшено, экспорт работает и без него. 

Добавить проверку вкладок, которые уже добавлены в базу данных через dynalinks.index. Для добавленных выводить кнопки в tab-list 
добавить / уже добавлена
удалить 

Проверить в чём была причина двойной попытки порта законнектиться. И решить проблему двойного коннекта в PortObjListener. Отлаживать порты вообще трудно. 
Подозреваю, при совпадении имён
if (port.name === this.name) {
второй порт надо либо посылать, либо добавлять в стек и слушать оба. 

Изменить дизайн списка вкладок. Сверху добавить панель с кнопками "Отправить" в категорию и "Закрыть" для массовых операций. Кнопки держать неактивными (disable), пока не выделена ни одна вкладка. 
//сделано

Форму для выбора категории и тега открывать в модальном диалоге, а не как сейчас, она рисуется сверху, сталкивая всё содержимое таблицы вниз. 

В форму добавить кнопки "Ок" и "Отмена", чтобы было понятно, отправлены вкладки в склад или нет. 

Добавить под кнопками панельку для вывода разных сообщений, например, о количестве открытых вкладок. Когда отмечаются вкладки, сообщать сколько вкладок было отмечено. После отправки сообщать, сколько было отправлено. 

Добавить кнопку "Убрать в склад", которая совмещает оба действия - отправить в хранилище и закрыть. 

Добавить для каждой вкладки кнопку "Отправить", которая открывает модальный диалог выбора категории и тега, как описано выше. 

Возможно, добавить функцию "Убрать в чулан", некая скрытая категория, куда будет валиться всё без разбору. 

Добавить возможность сохранять ВСЕ вкладки и назвать это "Сеанс". А также добавить функцию закрыть ВСЕ вкладки. Записать сеанс и закрыть. Добавить функцию "Восстановить сеанс", которая открывает все вкладки, записанные в данный сеанс. (Ужас). Выяснить, как открыть вкладки неактивными, без загрузки содержимого - возможно ли это?

Добавить поиск по фрагменту текста по адресу и по заголовку. Для этого нужна форма с двумя полями - одно для адреса, другое для заголовка. Если одно из них пустое, то второй поиск пропускается. Очень нужно. 

При сортировке по адресам "откусывать" протоколы, чтобы было нормальный порядок сортировки. 

Добавить, наконец, смену направления сортировки по всем параметрам. 

Проработать теоретическую возможность древовидных категорий. 

Проработать теоретическую возможность группировки вкладок по доменным именам первого уровня.


