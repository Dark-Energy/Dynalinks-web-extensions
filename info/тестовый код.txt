var ports = new Array(10);
function test_ppp()
{
    console.log("testing portman");
    function generator(reciver)
    {
       return function (port) {
            console.log("trying connect with " + reciver.name + " from " + port.name);
            //console.log("translator \n " + JSON.stringify(port));
            //console.log("reciver", JSON.stringify(reciver.port));
        };
        
    }

    for(var i=0;i<10; i++) {
        ports[i] = new Portman("test-ports-"+i, true);
        ports[i].post({"oh":"shit"});
    }

}

test_ppp();

тестирование Portman





var ports = new Array(10);


function test_mmm()
{

    function generator(reciver)
    {
       return function (port) {
            console.log("trying connect with " + reciver.name + " from " + port.name);
            //console.log("translator \n " + JSON.stringify(port));
            //console.log("reciver", JSON.stringify(reciver.port));
        };
        
    }

    for(var i=0;i<10; i++) {
        ports[i] = new PortObjListener("test-ports-"+i, true);
        ports[i].$on_connect_event(generator(ports[i]));
    }
    
}

test_mmm();


тестирование PortObjListener